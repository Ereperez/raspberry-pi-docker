services:
  gatus:
    image: ghcr.io/twin/gatus:v5.36.0
    container_name: gatus
    hostname: gatus
    restart: unless-stopped

    # Published on 8081 (not 8080) to match the tile already set up in
    # Homepage's services.yaml.
    ports:
      - '8081:8080'

    environment:
      TZ: ${TZ}
      HEALTHCHECKS_UUID: ${HEALTHCHECKS_UUID}
      WUD_BASIC_AUTH_B64: ${WUD_BASIC_AUTH_B64}
      GATUS_TAILSCALE_API_KEY: ${GATUS_TAILSCALE_API_KEY}
      GATUS_TAILSCALE_DEVICE_ID: ${GATUS_TAILSCALE_DEVICE_ID}

    volumes:
      - ./config/config.yaml:/config/config.yaml:ro
      - ./data:/data

    logging:
      driver: json-file
      options:
        max-size: '10m'
        max-file: '3'