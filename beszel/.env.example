# No quotes around values. Escape literal $ as $$. Save as LF line endings, not CRLF.
# Timezone
TZ=Europe/Stockholm
# Generated in the hub's web UI: Add System -> copy the key shown.
# The agent won't start (or won't authenticate) until this is set --
# expect it to sit unhealthy/erroring until you complete that step once.
BESZEL_AGENT_KEY=